extends ../layouts/layout_main.pug

append variables
  - const pageTitle = `Yialo's Reading Whitelist`;

block body
  body.page
    .page__wrapper(id='react-app')
    .page__overlay(id='page-overlay', role='presentation')
      .spinner

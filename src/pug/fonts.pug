- const { fonts } = require('@/fonts/fonts.json');
- for (const { fileNameBase, sets } of fonts)
  - for (const { weight, style } of sets)
    link(
      rel='preload',
      href=require(`@/fonts/${fileNameBase}_${weight}_${style}.woff2`).default,
      as='font',
      crossorigin='anonymous',
    )

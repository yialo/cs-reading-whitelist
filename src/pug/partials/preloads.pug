- const { fonts } = require('../../json/fonts.json');

- for (const { fileNameBase, sets } of fonts)
  - for (const { weight, style } of sets)
    link(
      rel='preload',
      href=`/assets/fonts/${fileNameBase}_${weight}_${style}.woff2`,
      as='font',
    )
